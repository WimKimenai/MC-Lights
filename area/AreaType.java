package wkimenai.mc-lights.area;

public enum AreaType
{
    PACKET_AREA("PACKET_AREA", 0), 
    VISUAL_AREA("VISUAL_AREA", 1);
    
    private AreaType(final String name, final int ordinal) {
    }
}
